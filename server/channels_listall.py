def channels_listall(token):
    # this is pretty simple, just grab all the channels from the database, reall
    # -y not a lot I can write at this point of the project
    channels = []
    
    return { "channels" : channels }

